# PocketSIEM Environment Configuration

# Backend API Configuration
# For local development, use your computer's IP address (not localhost)
# Get your IP: `ipconfig` (Windows) or `ifconfig` (Mac/Linux)
# Example: http://192.168.1.100:8080/api/v1
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1

# API Key - Must match the backend configuration
# Default backend key: your-custom-secret-key-here
# Change this in both frontend and backend for production
EXPO_PUBLIC_API_KEY=your-custom-secret-key-here

# Request timeout in milliseconds
EXPO_PUBLIC_API_TIMEOUT=10000

# Notes:
# 1. When testing on a physical device via Expo Go, replace localhost with your computer's IP
# 2. Ensure the backend server is running before starting the app
# 3. For Android emulator, use 10.0.2.2 instead of localhost
# 4. For iOS simulator, localhost works fine
